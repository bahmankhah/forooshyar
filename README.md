# فروشیار (Forooshyar) - API محصولات ووکامرس

افزونه فروشیار یک راه‌حل قدرتمند و قابل تنظیم برای ارائه API محصولات ووکامرس به سرویس‌های شخص ثالث است. این افزونه با استفاده از فریمورک WP-Lite ساخته شده و امکانات پیشرفته‌ای مانند کش هوشمند، پنل مدیریت فارسی، و مانیتورینگ API را ارائه می‌دهد.

## ویژگی‌های کلیدی

### 🚀 API محصولات پیشرفته
- **API RESTful کامل** با پشتیبانی از تمام عملیات محصولات
- **پاسخ‌های JSON ساختاریافته** با استفاده از JsonResource
- **سازگاری کامل با نسخه‌های قبلی** - بدون تغییر در ساختار JSON موجود
- **پشتیبانی از تنوع محصولات** با روابط parent-child
- **فیلترینگ و جستجوی پیشرفته** محصولات

### ⚡ سیستم کش هوشمند
- **کش خودکار** با TTL قابل تنظیم
- **ابطال خودکار کش** هنگام تغییر محصولات
- **کلیدهای کش منحصربه‌فرد** برای پارامترهای مختلف
- **آمار عملکرد کش** در پنل مدیریت
- **پشتیبانی از عملیات bulk** برای ابطال کش

### 🎛️ پنل مدیریت فارسی
- **رابط کاربری کاملاً فارسی** با پشتیبانی RTL
- **تنظیمات جامع** در 6 بخش مختلف
- **پیش‌نمایش زنده** تغییرات تنظیمات
- **صادرات و وارد کردن** تنظیمات
- **بازگردانی به پیش‌فرض** با تأیید

### 📊 مانیتورینگ و آنالیتیکس
- **مانیتور API زنده** با نمایش پاسخ‌های JSON
- **آمار استفاده** و عملکرد API
- **لاگ درخواست‌ها** با جزئیات کامل
- **محدودیت نرخ درخواست** (Rate Limiting)
- **تشخیص و مدیریت خطاها**

### 🛠️ قابلیت تنظیم بالا
- **قالب‌های عنوان سفارشی** با متغیرهای پویا
- **انتخاب فیلدهای API** - فعال/غیرفعال کردن هر فیلد
- **تنظیمات تصاویر** - اندازه، کیفیت، تعداد
- **پیکربندی کش** - مدت زمان، فعال‌سازی، ابطال خودکار
- **محدودیت‌های API** - حداکثر محصول، timeout، rate limit

## نصب و راه‌اندازی

### پیش‌نیازها
- WordPress 5.0 یا بالاتر
- WooCommerce 4.0 یا بالاتر
- PHP 7.4 یا بالاتر
- فریمورک WP-Lite

### مراحل نصب

1. **آپلود افزونه**
   ```bash
   # آپلود پوشه forooshyar به wp-content/plugins/
   ```

2. **نصب وابستگی‌ها**
   ```bash
   cd wp-content/plugins/forooshyar
   composer install
   ```

3. **فعال‌سازی افزونه**
   - به پنل مدیریت وردپرس بروید
   - از منوی "افزونه‌ها" گزینه "فروشیار" را فعال کنید

4. **تنظیمات اولیه**
   - به منوی "فروشیار > تنظیمات" بروید
   - تنظیمات مورد نظر خود را انجام دهید

## راهنمای استفاده

### 🔧 تنظیمات عمومی

#### دسترسی به تنظیمات
1. وارد پنل مدیریت وردپرس شوید
2. از منوی سمت راست روی "فروشیار" کلیک کنید
3. گزینه "تنظیمات" را انتخاب کنید

#### بخش‌های تنظیمات

**1. تنظیمات عمومی**
- **نمایش تنوع‌ها**: فعال/غیرفعال کردن نمایش تنوع محصولات
- **قالب عنوان**: تعریف قالب سفارشی برای عنوان محصولات
  ```
  مثال: {{product_name}} - {{variation_name}}
  متغیرهای موجود: {{product_name}}, {{variation_name}}, {{category}}, {{sku}}, {{brand}}
  ```
- **پسوند سفارشی**: متن اضافی برای انتهای عناوین
- **زبان پیش‌فرض**: انتخاب زبان رابط کاربری

**2. فیلدهای محصول**
انتخاب فیلدهایی که در پاسخ API نمایش داده شوند:
- ✅ عنوان (title)
- ✅ زیرعنوان (subtitle)  
- ✅ شناسه والد (parent_id)
- ✅ شناسه منحصربه‌فرد (page_unique)
- ✅ قیمت فعلی (current_price)
- ✅ قیمت قبلی (old_price)
- ✅ موجودی (availability)
- ✅ نام دسته‌بندی (category_name)
- ✅ لینک‌های تصاویر (image_links)
- ✅ لینک تصویر اصلی (image_link)
- ✅ آدرس صفحه (page_url)
- ✅ توضیح کوتاه (short_desc)
- ✅ مشخصات (spec)
- ✅ تاریخ (date)
- ✅ گارانتی (registry)
- ✅ ضمانت (guarantee)

**3. تنظیمات تصاویر**
- **اندازه‌های تصاویر**: انتخاب از thumbnail, medium, large, full
- **حداکثر تعداد تصاویر**: تعداد تصاویر در هر محصول (1-20)
- **کیفیت تصاویر**: درصد کیفیت فشرده‌سازی (50-100)

**4. تنظیمات کش**
- **فعال‌سازی کش**: روشن/خاموش کردن سیستم کش
- **مدت زمان کش (TTL)**: مدت نگهداری در کش (ثانیه)
- **ابطال خودکار**: پاک‌سازی خودکار هنگام تغییر محصولات

**5. محدودیت‌های API**
- **حداکثر محصول در صفحه**: تعداد محصولات قابل دریافت (10-200)
- **محدودیت نرخ**: تعداد درخواست مجاز در دقیقه (100-2000)
- **Timeout**: حداکثر زمان انتظار پاسخ (10-60 ثانیه)

**6. تنظیمات پیشرفته**
- **مدت نگهداری لاگ**: تعداد روزهای نگهداری لاگ‌ها
- **پاک‌سازی خودکار**: فعال‌سازی پاک‌سازی خودکار لاگ‌ها
- **مانیتورینگ عملکرد**: ثبت آمار عملکرد API

### 📊 مانیتور API

#### دسترسی به مانیتور
1. از منوی "فروشیار" گزینه "مانیتور API" را انتخاب کنید
2. صفحه مانیتورینگ زنده باز می‌شود

#### امکانات مانیتور

**1. تست API**
- انتخاب endpoint مورد نظر
- تنظیم پارامترهای درخواست
- مشاهده پاسخ JSON با syntax highlighting
- نمایش زمان پاسخ و وضعیت کش

**2. آمار زنده**
- تعداد کل درخواست‌ها
- میانگین زمان پاسخ
- نرخ موفقیت کش (Cache Hit Rate)
- آمار خطاها

**3. لاگ درخواست‌ها**
- مشاهده آخرین درخواست‌ها
- جزئیات IP، endpoint، پارامترها
- زمان پاسخ و وضعیت

### 🔗 استفاده از API

#### Endpoints موجود

**1. دریافت لیست محصولات**
```http
GET /wp-json/wplite/v1/products
```

پارامترهای اختیاری:
- `page`: شماره صفحه (پیش‌فرض: 1)
- `limit`: تعداد محصولات (پیش‌فرض: 100، حداکثر: 200)
- `show_variations`: نمایش تنوع‌ها (true/false)

مثال:
```http
GET /wp-json/wplite/v1/products?page=1&limit=50&show_variations=true
```

**2. دریافت محصول خاص**
```http
GET /wp-json/wplite/v1/products/{id}
```

مثال:
```http
GET /wp-json/wplite/v1/products/123
```

**3. دریافت محصولات با ID**
```http
POST /wp-json/wplite/v1/products/by-ids
Content-Type: application/json

{
    "ids": [123, 456, 789],
    "show_variations": true
}
```

**4. دریافت محصولات با Slug**
```http
POST /wp-json/wplite/v1/products/by-slugs
Content-Type: application/json

{
    "slugs": ["product-1", "product-2"],
    "show_variations": false
}
```

#### ساختار پاسخ JSON

```json
{
    "count": 150,
    "max_pages": 3,
    "products": [
        {
            "title": "نام محصول",
            "subtitle": "زیرعنوان محصول",
            "parent_id": 0,
            "page_unique": 123,
            "current_price": "250000",
            "old_price": "300000",
            "availability": "موجود",
            "category_name": "الکترونیک",
            "image_links": ["url1.jpg", "url2.jpg"],
            "image_link": "main-image.jpg",
            "page_url": "https://site.com/product/sample",
            "short_desc": "توضیح کوتاه محصول",
            "spec": [
                {
                    "رنگ": "آبی",
                    "سایز": "بزرگ",
                    "برند": "نمونه"
                }
            ],
            "date": {
                "date": "2024-12-06 12:00:00",
                "timezone_type": 3,
                "timezone": "Asia/Tehran"
            },
            "registry": "گارانتی 2 ساله",
            "guarantee": "ضمانت بازگشت 7 روزه"
        }
    ]
}
```

### 🔄 مدیریت کش

#### مشاهده وضعیت کش
- در بخش "تنظیمات > کش" وضعیت فعلی کش را مشاهده کنید
- در "مانیتور API" آمار Cache Hit Rate را ببینید

#### پاک‌سازی دستی کش
```php
// در کد PHP
$cacheService = new \Forooshyar\Services\CacheService($configService);
$cacheService->flush(); // پاک کردن کل کش
$cacheService->invalidateProduct(123); // پاک کردن کش محصول خاص
```

#### تنظیم TTL کش
1. به "تنظیمات > کش" بروید
2. مقدار "مدت زمان کش" را تنظیم کنید (بر حسب ثانیه)
3. تنظیمات را ذخیره کنید

### 📈 مانیتورینگ و عیب‌یابی

#### مشاهده لاگ‌ها
1. به "مانیتور API" بروید
2. بخش "لاگ درخواست‌ها" را مشاهده کنید
3. برای جزئیات بیشتر روی هر درخواست کلیک کنید

#### عیب‌یابی مشکلات رایج

**مشکل: API پاسخ نمی‌دهد**
- بررسی کنید WooCommerce فعال باشد
- تنظیمات permalink را بررسی کنید
- لاگ خطاهای وردپرس را چک کنید

**مشکل: کش کار نمی‌کند**
- در تنظیمات کش را فعال کنید
- TTL را بررسی کنید (نباید 0 باشد)
- مجوزهای نوشتن در دیتابیس را چک کنید

**مشکل: تصاویر نمایش داده نمی‌شوند**
- تنظیمات تصاویر را بررسی کنید
- مجوزهای فایل‌های تصاویر را چک کنید
- اندازه‌های تصاویر تعریف شده را بررسی کنید

### 🔒 امنیت و محدودیت‌ها

#### Rate Limiting
- در "تنظیمات > محدودیت‌های API" نرخ مجاز را تنظیم کنید
- سیستم خودکار IP های مشکوک را محدود می‌کند
- لاگ تمام درخواست‌ها ثبت می‌شود

#### محدودیت دسترسی
- API عمومی است و نیاز به احراز هویت ندارد
- برای محدود کردن دسترسی از firewall یا plugin امنیتی استفاده کنید
- IP whitelist/blacklist در تنظیمات پیشرفته قابل تنظیم است

### 📤 صادرات و وارد کردن تنظیمات

#### صادرات تنظیمات
1. به "تنظیمات" بروید
2. دکمه "صادرات تنظیمات" را کلیک کنید
3. فایل JSON دانلود می‌شود

#### وارد کردن تنظیمات
1. دکمه "وارد کردن تنظیمات" را کلیک کنید
2. فایل JSON صادر شده را انتخاب کنید
3. تنظیمات جدید اعمال می‌شود

---

## راهنمای توسعه‌دهندگان

### 🏗️ معماری سیستم

#### ساختار کلی
فروشیار بر اساس معماری MVC و اصول Clean Architecture ساخته شده است:

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   REST API      │    │   Admin Panel    │    │  Cache Layer    │
│   Endpoints     │    │   (Persian UI)   │    │  (Transients)   │
└─────────┬───────┘    └─────────┬────────┘    └─────────┬───────┘
          │                      │                       │
          ▼                      ▼                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Controllers Layer                            │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ ProductController│  │ AdminController │  │ MonitorController│ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Services Layer                               │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ ProductService  │  │ ConfigService   │  │ CacheService    │ │
│  │                 │  │                 │  │                 │ │
│  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │ ┌─────────────┐ │ │
│  │ │ TitleBuilder│ │  │ │ Validator   │ │  │ │ Invalidator │ │ │
│  │ └─────────────┘ │  │ └─────────────┘ │  │ └─────────────┘ │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Resources Layer                              │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ ProductResource │  │ ProductCollection│  │ ErrorResource   │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Data Layer                                   │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ WooCommerce API │  │ WordPress DB    │  │ Cache Storage   │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 📁 ساختار فایل‌ها

#### فایل‌های اصلی
```
forooshyar/
├── forooshyar.php              # فایل اصلی افزونه
├── composer.json               # وابستگی‌های PHP
├── configs/                    # فایل‌های پیکربندی
│   ├── app.php                # تنظیمات اصلی اپلیکیشن
│   ├── defaults.php           # مقادیر پیش‌فرض
│   ├── localization.php       # تنظیمات زبان
│   └── plugin.php             # تنظیمات افزونه
├── src/                       # کدهای اصلی
│   ├── Controllers/           # کنترلرها
│   ├── Services/              # سرویس‌ها
│   ├── Resources/             # منابع JSON
│   ├── Middleware/            # میان‌افزارها
│   └── Providers/             # ارائه‌دهندگان سرویس
├── routes/                    # مسیریابی
│   ├── rest.php              # مسیرهای REST API
│   ├── admin.php             # مسیرهای پنل مدیریت
│   └── ajax.php              # مسیرهای AJAX
├── views/                     # قالب‌های نمایش
│   └── admin/                # قالب‌های پنل مدیریت
├── assets/                    # فایل‌های استاتیک
│   ├── css/                  # فایل‌های CSS
│   └── js/                   # فایل‌های JavaScript
├── languages/                 # فایل‌های ترجمه
├── tests/                     # تست‌ها
│   ├── Feature/              # تست‌های ویژگی
│   └── Unit/                 # تست‌های واحد
└── vendor/                    # وابستگی‌های Composer
```

### 🔧 کلاس‌های اصلی

#### ProductController
```php
namespace Forooshyar\Controllers;

class ProductController extends Controller
{
    public function index(WP_REST_Request $request): WP_REST_Response
    public function show(WP_REST_Request $request): WP_REST_Response
    public function getByIds(WP_REST_Request $request): WP_REST_Response
    public function getBySlugs(WP_REST_Request $request): WP_REST_Response
}
```

#### ProductService
```php
namespace Forooshyar\Services;

class ProductService 
{
    public function getProducts(array $args): array
    public function getProductsFromIds(array $productIds): array
    public function getProductsFromSlugs(array $slugs): array
    private function extractProductValues(WC_Product $product): stdClass
}
```

#### ConfigService
```php
namespace Forooshyar\Services;

class ConfigService
{
    public function get(string $key, $default = null)
    public function set(string $key, $value): bool
    public function getAll(): array
    public function reset(): bool
    public function validateTitleTemplate(string $template): array
}
```

#### CacheService
```php
namespace Forooshyar\Services;

class CacheService
{
    public function get(string $key)
    public function set(string $key, $value, ?int $ttl = null): bool
    public function delete(string $key): bool
    public function invalidateProduct(int $productId): bool
    public function generateKey(string $prefix, array $params): string
}
```

### 🛠️ توسعه و سفارشی‌سازی

#### اضافه کردن فیلد جدید به API

1. **تعریف فیلد در defaults.php**
```php
'fields' => [
    // فیلدهای موجود...
    'new_field' => true,
]
```

2. **اضافه کردن به ProductResource**
```php
public function toArray(): array
{
    $data = [
        // فیلدهای موجود...
    ];
    
    if ($this->shouldIncludeField('new_field')) {
        $data['new_field'] = $this->getNewFieldValue();
    }
    
    return $data;
}
```

3. **اضافه کردن به تنظیمات UI**
در `views/admin/partials/fields-tab.view.php`:
```php
<label>
    <input type="checkbox" name="fields[new_field]" value="1" 
           <?php checked($config['fields']['new_field'] ?? true); ?>>
    <?php _e('فیلد جدید', 'forooshyar'); ?>
</label>
```

#### ایجاد سرویس جدید

1. **ایجاد کلاس سرویس**
```php
<?php
namespace Forooshyar\Services;

class NewService
{
    private ConfigService $configService;
    
    public function __construct(ConfigService $configService)
    {
        $this->configService = $configService;
    }
    
    public function doSomething(): array
    {
        // پیاده‌سازی منطق
    }
}
```

2. **ثبت در AppServiceProvider**
```php
public function register(): void
{
    $this->app->singleton(NewService::class, function ($app) {
        return new NewService($app->make(ConfigService::class));
    });
}
```

#### اضافه کردن middleware جدید

1. **ایجاد کلاس middleware**
```php
<?php
namespace Forooshyar\Middleware;

class NewMiddleware
{
    public function handle($request, $next)
    {
        // منطق middleware
        return $next($request);
    }
}
```

2. **ثبت در app.php**
```php
'global_middlewares' => [
    NewMiddleware::class,
],
```

### 🧪 تست‌ها

#### ساختار تست‌ها
```
tests/
├── Feature/                   # تست‌های ویژگی (Integration)
│   ├── ApiLoggingMiddlewareTest.php
│   ├── CacheBehaviorTest.php
│   ├── ProductDataExtractionTest.php
│   └── ...
├── Unit/                      # تست‌های واحد
│   ├── ProductControllerTest.php
│   ├── ProductServiceRefactorTest.php
│   └── ...
├── Pest.php                   # پیکربندی Pest
├── TestCase.php              # کلاس پایه تست
└── bootstrap.php             # راه‌اندازی تست‌ها
```

#### اجرای تست‌ها

**اجرای تمام تست‌ها:**
```bash
cd forooshyar
./vendor/bin/pest
```

**اجرای تست‌های ویژگی:**
```bash
./vendor/bin/pest tests/Feature
```

**اجرای تست‌های واحد:**
```bash
./vendor/bin/pest tests/Unit
```

**اجرای تست خاص:**
```bash
./vendor/bin/pest tests/Feature/ProductDataExtractionTest.php
```

**اجرای تست‌ها با coverage:**
```bash
./vendor/bin/pest --coverage
```

#### نوشتن تست جدید

**تست ویژگی:**
```php
<?php

use Forooshyar\Services\ProductService;

it('should return products with correct structure', function () {
    $productService = new ProductService($configService, $titleBuilder);
    
    $result = $productService->getProducts([
        'limit' => 10,
        'page' => 1
    ]);
    
    expect($result)->toHaveKeys(['count', 'max_pages', 'products']);
    expect($result['products'])->toBeArray();
});
```

**تست Property-Based:**
```php
it('should maintain response structure for any valid configuration', function () {
    $config = configurationGenerator();
    
    // تست با تنظیمات تصادفی
    $result = testApiWithConfig($config);
    
    expect($result)->toHaveValidStructure();
})->repeat(100);
```

#### تست‌های موجود

**تست‌های ویژگی:**
- `ApiLoggingMiddlewareTest`: تست middleware لاگ API
- `CacheBehaviorTest`: تست رفتار کش
- `ProductDataExtractionTest`: تست استخراج داده محصولات
- `ResponseTransformationTest`: تست تبدیل پاسخ‌ها
- `TitleTemplateProcessingTest`: تست پردازش قالب عنوان

**تست‌های Property-Based:**
- تست سازگاری پاسخ‌ها
- تست رفتار کش
- تست قالب‌های عنوان
- تست تنظیمات فیلدها
- تست ساختار JSON

### 🔍 عیب‌یابی و لاگ‌ها

#### فعال‌سازی حالت Debug
در `wp-config.php`:
```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
```

#### مشاهده لاگ‌ها
```bash
# لاگ‌های وردپرس
tail -f wp-content/debug.log

# لاگ‌های افزونه
tail -f wp-content/plugins/forooshyar/logs/wplite-errors.log
```

#### لاگ‌گذاری سفارشی
```php
use Forooshyar\Services\LoggingService;

$logger = app(LoggingService::class);
$logger->info('پیام اطلاعاتی', ['context' => 'data']);
$logger->error('پیام خطا', ['error' => $exception]);
```

### 📊 مانیتورینگ عملکرد

#### آمار کش
```php
$cacheService = app(CacheService::class);
$stats = $cacheService->getStats();
// ['hits' => 150, 'misses' => 50, 'hit_rate' => 75.0]
```

#### آمار API
```php
$apiLogService = app(ApiLogService::class);
$stats = $apiLogService->getStats();
// ['total_requests' => 1000, 'avg_response_time' => 250, ...]
```

### 🚀 بهینه‌سازی عملکرد

#### تنظیمات کش بهینه
```php
'cache' => [
    'enabled' => true,
    'ttl' => 3600,        // 1 ساعت برای محصولات
    'auto_invalidate' => true,
]
```

#### تنظیمات دیتابیس
```sql
-- ایندکس‌های مفید برای عملکرد بهتر
CREATE INDEX idx_product_status ON wp_posts(post_status, post_type);
CREATE INDEX idx_product_date ON wp_posts(post_date, post_type);
```

#### محدودیت‌های API
```php
'api' => [
    'max_per_page' => 100,    // حداکثر محصول در صفحه
    'rate_limit' => 1000,     // درخواست در ساعت
    'timeout' => 30,          // ثانیه
]
```

### 🔄 CI/CD و Deployment

#### GitHub Actions
```yaml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.0
      - name: Install dependencies
        run: composer install
      - name: Run tests
        run: ./vendor/bin/pest
```

#### مراحل انتشار
1. اجرای تست‌ها
2. بررسی کیفیت کد
3. ساخت نسخه production
4. آپلود به سرور

---

## 📞 پشتیبانی و مشارکت

### گزارش مشکلات
- مشکلات را در GitHub Issues گزارش دهید
- لاگ‌های مربوطه را ضمیمه کنید
- مراحل بازتولید مشکل را شرح دهید

### مشارکت در توسعه
1. Fork کردن پروژه
2. ایجاد branch جدید
3. اعمال تغییرات
4. نوشتن تست‌ها
5. ارسال Pull Request

### مجوز
این افزونه تحت مجوز MIT منتشر شده است.

---

**نسخه:** 2.0.0  
**آخرین بروزرسانی:** دی ۱۴۰۳  
**سازگاری:** WordPress 5.0+, WooCommerce 4.0+, PHP 7.4+