# وظایف و مشکلات پروژه فروشیار

## ✅ مشکلات رفع شده

### 1. مشکل توقف کار تحلیل (Analysis Job)
- **مشکل**: کار تحلیل پس از رفرش صفحه متوقف می‌شد
- **علت**: استفاده از WordPress Cron که فقط با بازدید صفحه اجرا می‌شود
- **راه‌حل**: 
  - ذخیره وضعیت کار در جدول `aiagent_scheduled` برای پایداری
  - استفاده از WordPress Heartbeat API برای نظارت مداوم
  - اضافه کردن مکانیزم بازیابی کارهای متوقف شده
  - ارسال درخواست async برای اطمینان از اجرای cron

### 2. جدول `aiagent_context` استفاده نشده ✅
- **مشکل**: جدول ایجاد می‌شد اما هیچ‌جا استفاده نمی‌شد
- **راه‌حل**: 
  - ایجاد سرویس `ContextManager` در `src/Modules/AIAgent/Services/ContextManager.php`
  - مدیریت پرامپت‌ها و قالب‌های سیستم
  - پشتیبانی از import/export
  - قابلیت reset به مقادیر پیش‌فرض
  - کش داخلی برای بهبود عملکرد

### 3. جدول `aiagent_scheduled` استفاده ناقص ✅
- **مشکل**: فقط برای ذخیره وضعیت job استفاده می‌شد
- **راه‌حل**:
  - ایجاد سرویس `ScheduledTaskService` در `src/Modules/AIAgent/Services/ScheduledTaskService.php`
  - پشتیبانی از انواع وظایف:
    - `price_change`: تغییرات قیمت زمان‌بندی شده
    - `followup`: پیگیری‌های زمان‌بندی شده
    - `campaign`: کمپین‌های زمان‌بندی شده
    - `inventory_check`: بررسی موجودی
  - بروزرسانی `ScheduleFollowupAction` و `SchedulePriceChangeAction` برای استفاده از سرویس جدید
  - پردازش خودکار وظایف سررسید شده با WordPress Cron

---

## 🟡 بهبودهای پیشنهادی (اولویت متوسط)

### 4. یکپارچه‌سازی ContextManager با Analyzers
- **وضعیت**: سرویس ایجاد شده اما هنوز در ProductAnalyzer و CustomerAnalyzer استفاده نشده
- **پیشنهاد**: 
  - استفاده از `ContextManager::buildSystemPrompt()` در analyzers
  - امکان سفارشی‌سازی پرامپت‌ها از طریق پنل مدیریت

### 5. افزودن UI برای مدیریت وظایف زمان‌بندی شده
- **وضعیت**: سرویس backend آماده است
- **پیشنهاد**:
  - صفحه مدیریت وظایف زمان‌بندی شده در پنل ادمین
  - نمایش لیست وظایف pending
  - امکان لغو وظایف
  - نمایش تاریخچه اجرا

### 6. افزودن UI برای مدیریت Context/Prompts
- **وضعیت**: سرویس backend آماده است
- **پیشنهاد**:
  - صفحه ویرایش پرامپت‌های سیستم
  - امکان ایجاد قالب‌های ایمیل و پیامک سفارشی
  - import/export تنظیمات

---

## 🔵 ویژگی‌های جدید (اولویت پایین)

### 7. سیستم قالب‌های ایمیل و پیامک
- استفاده از `ContextManager` برای ذخیره قالب‌ها
- متغیرهای پویا مانند `{{customer_name}}`, `{{product_name}}`
- پیش‌نمایش قالب قبل از ارسال

### 8. گزارش‌گیری از وظایف زمان‌بندی شده
- آمار اجرای وظایف
- نرخ موفقیت/شکست
- میانگین زمان اجرا

---

## 📝 یادداشت‌های فنی

### سرویس‌های جدید اضافه شده:
1. **ContextManager** (`src/Modules/AIAgent/Services/ContextManager.php`)
   - مدیریت پرامپت‌ها و قالب‌ها
   - ذخیره در جدول `aiagent_context`
   - متدهای اصلی: `get()`, `save()`, `buildSystemPrompt()`, `getEmailTemplate()`, `getSmsTemplate()`

2. **ScheduledTaskService** (`src/Modules/AIAgent/Services/ScheduledTaskService.php`)
   - مدیریت وظایف زمان‌بندی شده
   - ذخیره در جدول `aiagent_scheduled`
   - متدهای اصلی: `schedule()`, `schedulePriceChange()`, `scheduleFollowup()`, `processDueTasks()`

### تغییرات در فایل‌های موجود:
- `AIAgentModule.php`: ثبت سرویس‌های جدید و initialization
- `ScheduleFollowupAction.php`: استفاده از ScheduledTaskService
- `SchedulePriceChangeAction.php`: استفاده از ScheduledTaskService
